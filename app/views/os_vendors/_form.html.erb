<%= form_for(@os_vendor) do |f| %>
  <% if @os_vendor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@os_vendor.errors.count, "error") %> prohibited this os_vendor from being saved:</h2>

      <ul>
      <% @os_vendor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
